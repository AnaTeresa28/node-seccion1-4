# Notas
Este es mi primer programa en Node
```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Base de cálculo de la tabla de multiplicar  [number] [required]
  -l, --listar   Lista la tabla en consola [boolean] [required] [default: false]
  -m, --maximo   Número máximo de cálculo                    [number] [required]
```